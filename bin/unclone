#!/usr/bin/env bash
# Usage: unclone <folder>
TARGET="$1"
ABS_PATH=$(realpath "$TARGET")
REPO_LIST="$HOME/.local/share/repos.txt"

sed -i "\|^$ABS_PATH$|d" "$REPO_LIST"

command -v trash &>/dev/null && trash "$TARGET" || rm -rf "$TARGET"
echo "Uncloned: $TARGET"
